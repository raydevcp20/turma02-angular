import { FormatCEPPipe } from './format-cep.pipe';

describe('FormatCEPPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCEPPipe();
    expect(pipe).toBeTruthy();
  });
});
